name: publish-python

on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  publish:
    uses: quanttide/.github/workflows/pypi-publish.yml@v1
    with:
      python-version: "3.12"
      working-directory: packages/python
      tag-prefix: python/
    secrets: inherit
